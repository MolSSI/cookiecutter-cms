package:
  name: {{cookiecutter.repo_name}}
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - {{cookiecutter.repo_name}}

about:
  home: add_url_here
  license: {{cookiecutter.open_source_license}} License
